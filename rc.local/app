START=10 
STOP=15

boot() {
        echo boot
        # commands to run on boot
}

start() {        
        echo start
        # commands to launch application
}                 

stop() {          
        echo stop
        # commands to kill application 
}